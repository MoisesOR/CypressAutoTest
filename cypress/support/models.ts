export class ExampleData{
    public name: string
    public email: string
    public body: string
    public search: string 

    constructor(name: string, email: string, body: string, search: string){
        this.name = name
        this.email = email
        this.body = body
        this.search = search
    }
}